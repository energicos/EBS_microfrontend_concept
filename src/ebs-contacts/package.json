{
  "name": "ebs-contacts",
  "version": "1.0.0",
  "description": "My tiptop Nuxt.js project",
  "author": "pavel.yeliseev",
  "private": true,
  "scripts": {
    "dev": "cross-env PORT=3001 NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.0.0",
    "cors": "^2.8.5",
    "cross-env": "^5.2.0",
    "express": "^4.16.3",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
